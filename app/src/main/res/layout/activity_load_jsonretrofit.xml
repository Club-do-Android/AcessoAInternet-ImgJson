<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_load_jsonretrofit"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.pedro.jsonimg.LoadJSONRetrofit">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:id="@+id/linearLayout">
        <EditText
            android:hint="ID de um post"
            android:id="@+id/etIdPost"
            android:layout_width="280dp"
            android:layout_height="match_parent" />
        <Button
            android:id="@+id/btListar"
            android:text="Listar"
            android:layout_width="wrap_content"
            android:layout_height="match_parent" />
    </LinearLayout>

    <TextView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:text="Carregando JSON(Retrofit)..."
        android:id="@+id/tvRetrofit"
        android:layout_below="@+id/linearLayout" />

</RelativeLayout>
